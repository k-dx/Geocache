<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" media="print" href="/styles/output.css" >
    <title>Geocache waypoint</title>
    <style>
        .info-message {
            color: rgb(185 28 28);
            font-size: 1.875rem;
            font-weight: bold;
            margin: 2rem auto;
            width: fit-content;
        }
        .print-button {
            padding: 10px 20px; 
            font-size: 1.5rem;
        }
        @media print {
            .info-message {
                display: none !important;
            }
        }
    </style>
</head>
<body>
    <div class="info-message">
        Please print this page. It will look correctly only when printed. This message will not be printed. 
        <button class="print-button" onclick="window.print()">Click to print</button>
    </div>
    <div class="p-4">
        <h1 class="text-4xl font-bold mb-4">Geocache waypoint</h1>
        <h2 class="text-3xl mb-2">Route: <%= locals.route.name %></h2>
        <h2 class="text-3xl mb-2">Waypoint: <%= locals.waypoint.name %></h2>
        <h3 class="text-3xl mb-2">Scan the QR Code or go to link below.</h3>
        <img src="<%= locals.qr_img %>" alt="qr code" class="mx-auto w-3/4">
        <div class="w-full text-center text-2xl">
            <%= locals.waypointVisitLink %>
        </div>
    </div>
    <script>
        print()
    </script>
</body>
</html>